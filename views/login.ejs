<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campeonatos - Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Estilo Customizado -->
    <style>
        body {
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .login-container {
            background: rgba(255, 255, 255, 0.1);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            max-width: 350px;
            width: 100%;
        }

        .login-container img {
            width: 80px;
            margin-bottom: 20px;
        }

        h2 {
            font-size: 26px;
            margin-bottom: 20px;
        }

        .form-control {
            background: rgba(255, 255, 255, 0.2);
            border: none;
            border-radius: 10px;
            padding: 15px;
            color: #fff;
        }

        .form-control::placeholder {
            color: #ddd;
        }

        .form-control:focus {
            box-shadow: 0 0 10px rgba(255, 180, 0, 0.5);
            border: none;
            outline: none;
        }

        .btn-custom {
            background-color: #FFB400;
            color: #fff;
            border: none;
            padding: 12px;
            border-radius: 10px;
            width: 100%;
            font-size: 16px;
            transition: 0.3s;
        }

        .btn-custom:hover {
            background-color: #eaa100;
        }

        .text-center a {
            color: #FFB400;
            text-decoration: none;
        }

        .text-center a:hover {
            text-decoration: underline;
        }

        .fade-in {
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>

    <div class="login-container fade-in">
        <!-- Logo -->
        <img src="/campeonato.png" alt="Logo" class="d-block mx-auto">

        <!-- Formulário de Login -->
        <h2 class="text-center">Bem-vindo</h2>
        <form id="loginForm">
            <!-- Campo de email -->
            <div class="mb-4">
                <input type="email" class="form-control" id="email" placeholder="Digite seu email" required>
            </div>

            <!-- Campo de senha -->
            <div class="mb-4">
                <input type="password" class="form-control" id="password" placeholder="Digite sua senha" required>
            </div>

            <!-- Botão de login -->
            <button type="submit" class="btn btn-custom">Entrar</button>
        </form>

        <!-- Links de ajuda -->
        <div class="text-center mt-3">
            <a href="#">Esqueceu a senha?</a> | <a href="/cadastro">Criar conta</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- JavaScript para Redirecionamento -->
    <script>
        document.getElementById('loginForm').addEventListener('submit', function (event) {
            event.preventDefault();

            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            // Simulação de login (substitua com sua lógica de validação)
          //  if (email === 'usuario@exemplo.com' && password === '123456') {
                // Redirecionar para o dashboard após login bem-sucedido
                window.location.href = 'admin/campeonato/lst';
         //   } else {
            //    alert('Email ou senha incorretos. Tente novamente.');
        //    }
        });
    </script>
</body>

</html>
